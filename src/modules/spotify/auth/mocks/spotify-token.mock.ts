import { SpotifyToken } from '../types/spotify'

export const spotifyTokenMock: SpotifyToken = {
  access_token: 'accessToken',
  token_type: 'tokenType',
  expires_in: 3600,
  refresh_token: 'refreshToken',
}
